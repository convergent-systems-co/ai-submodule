[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: Unit tests for individual functions
    integration: End-to-end pipeline tests
    schema: Schema validation tests
    artifacts: Governance artifact integrity tests
